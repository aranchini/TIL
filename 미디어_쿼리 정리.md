# 반응형 & 레이아웃
## 1. 반응형(Media Query)
> 화면크기에 따라 웹 페이지가 자동으로 레이아웃을 바꾸는 것 
- **@media**<br>
    반응형 웹 디자인을 구현하기 위해 사용하는 CSS의 기술
    - 주요 사용 목적<br>
        1. 다양한 기기 대응:스마트폰,태블릿,PC등 기기의 화면에 맞춰 레이아웃을 유연하게 변경 가능
        2. 사용자 경험 최적화:좁은 모바일 화면에서는 요소를 세로 배치, 넓은 PC화면에서는 가로로 배치 하는 등 화면에 최적화 된 형태를 보여줌
``` 
/*기본 스타일 (모바일 화면 등 좁은 화면)*/
body {
  background-color: lightblue;
}

/*화면 너비가 768px 이상일 때 적용되는 스타일 (태블릿 및 PC)*/
@media(min-width: 768px) {
  body {
    background-color: lightgreen;
  }
}
```

- 1. **max-width(최대 너비)**<br>
    >화면의 너비가 설정한 값 **이하**일 때만 스타일이 적용됨
- 조건: 화면 너비 <= 설정값 
```
@media (max-width: 768px) {
  body {
    background-color: lightblue;
    /*화면 너비가 768px 이하일 때 적용*/
  }
}
```
- 2. **min-width(최소 너비)**<br>
    >화면의 너비가 설정한 값 **이상**일 때만 스타일이 적용됨
- 조건: 화면너비 >= 설정 값
```
@media (min-width: 769px) {
  body {
    background-color: lightgreen;   
    /*화면 너비가 769px 이상일 때 적용*/
  }
}
```
3. **두개 비교**

|속성|조건|일반적인 용도|작동 방식|
|:---|:----|:---------|:--------|
|`max-width`|화면 크기가 **특정 값 이하**|모바일 화면 대응| 화면을 줄여나가다가 설정 값에 도달하면 작동|
|`min-width`|화면 크기가 **특정 값 이상**|PC 화면 대응|화면을 넓혀가다가 설정 값에 도달하면 작동|

4. **두 속성의 조합**<br>
>두 속성을 함께 사용하면 **특정 화면 크기 구간**에서만 작동하는 스타일을 만들수 있음
```
/*화면 너비가 480px 이상이고 768px 이하일 때만 적용*/
@media (min-width: 480px) and (max-width: 768px) {
  body {
    background-color: lightyellow; 
  }
}
```
- **반응형 단위**<br>
    
    >화면 크기에 맞춰 유연하게 크기가 변하는 반응형 웹을 만들기 위해 사용하는 핵심 단위들<br>
---
1. **```%```(퍼센트)**
- 기준:**부모요소**의 크기를 기준으로 함
- 부모요소의 너비나 높이가 변하면 그에 비례하여 자식요소의 크기도 함께 변함
```
.box {
  width: 50%;  /*부모 요소 너비의 절반 크기*/
  height: 100px;
}
```
2. **```vw```(Viewport Width)**
- 기준:**화면(뷰포트)의 전체 너비**를 기준으로 함
- 1vw는 화면 너비의 1%를 의미. 화면이 넓어지면 단위의 크기도 함께 커짐
```
.width{
  width: 100vw; /*화면 전체 너비*/
}
```
3. **```vh```(Viewport Height)**<br>
- 기준: **화면(뷰포트)의 전체 높이**를 기준으로 함
- 1vh는 화면 높이의 1%를 의미. 모바일이나 PC등 기기 높이에 딱 맞춘 화면을 구성할 때 유용함
> 뷰포트란?<br>사용자가 **실제로 웹페이지를 볼 수 있는 화면 영역**을 의미
```
.height{
  height: 100vh; /*화면 전체 높이*/
}
```
4. **`rem`(Root EM)**<br>
- 기준:**최상위 요소(루트,`<html>`)의 폰트 크기**를 기준으로 함
- 보통 브라우저의 기본 글자 크기인 `16px`이 **1rem**이 됨.
- 부모 요소의 영향을 받지 않고 일관된 크기를 유지할수 있어 반응형 타이포그래피에 자주 쓰임
>타이포 그래피란?<br> 글자를 배열하고 디자인하여 시각적인 커뮤니케이션을 완성하는 기술과 예술을 의미
```
html {
  font-size: 16px; /* 1rem = 16px */
}
.text {
  font-size: 1.5rem; /* 16px * 1.5 = 24px */
}
```
## 2. 레이아웃
1. **Flex(Flexible Box)**
> 웹 페이지의 레이아웃을 배치하고 정렬하기 위한 **1차원 방식의 CSS 레이아웃 방식**
- **Flex Container(컨테이너)**:Flex 속성을 적용할 부모 요소<br>
`display:flex;`를 선언하여 활성화 함
- **Flex Item(아이템)**:컨테이너 안에 들어 있는 자식 요소들
- **주축(Main Axis)**:아이템들이 배치되는 기본 방향(기본값은 가로 방향임)
- **교차축(Cross Axis):주축과 수직으로 교차하는 방향(기본값은 세로 방향임)
---
  1. **`display:flex;`(컨테이너를 Flex 박스로 선언)**<br>
- 역할:부모 요소를 Flex 컨테이너로 만들어 내부의 자식요소(Flex Item)들을 유연하게 배치할 수 있도록 준비 시킴
- 이 속성을 선언 해야만 아래의 정렬 속성들을 자식 요소에 적용 가능
```
.container {
  display: flex;
}
```
  2. **`justify-content`(주축(Main Axis)정렬)**<br>
- 역할:아이템들이 배치되는 방향인 주축을 기준으로 아이템들을 어떻게 정렬할지 결정함
- 주요 속성 값:
    - `flex-start`:시작점 정렬
    - `flex-end`:끝점 정렬
    - `center`:가운데 정렬
    - `space-between`:첫번째와 마지막 아이템을 양 끝에 붙이고 나머지 공간을 균등하게 분할하여 배치
    - `space-around`:아이템 양옆의 공간을 동일하게 분할하여 배치
  3. **`align-items`(교차축(Cross Axis)정렬)**<br>
- 역할:주축과 수직으로 교차하는 방향인 교차축을 기준으로 아이템들의 정렬을 결정함
- 주요 속성 값:
    - `stretch`:아이템들의 높이 또는 너비를 컨테이너 크기에 맞게 늘림
    - `flex-start`:교차축의 시작점 정렬
    - `flex-end`:교차축의 끝점 정렬
    - `center`:교차축의 가운데 정렬
    - `baseline`:아이템 내부의 텍스트 베이스라인을 기준으로 정렬

    4. **실전 활용 예시**
```
  .container {
  display: flex;                /* 1. Flex 컨테이너 생성 */
  justify-content: center;      /* 2. 주축(가로) 방향 중앙 정렬 */
  align-items: center;          /* 3. 교차축(세로) 방향 중앙 정렬 */
  height: 100vh;
}
```
2. **`Grid`**<br>
>웹 페이지의 레이아웃을 배치하고 정렬하기 위한 **2차원(행과열)방식의 CSS 레이아웃 방식**
- **Grid container(컨테이너)**:Grid 속성을 적용할 부모요소. `display:grid;`를 선언하여 활성화 함
- **Grid Item(아이템)**:컨테이너 안에 들어있는 자식 요소
---
  1. **`display:grid;`(컨테이너 선언)**
  - 역할:부모요소를 Grid 컨테이너로 만듬
  2. **`grid-template-columns` / `grid-template-rows`**
  - 역할:격자의 열(Column)과 행(Row)의 크기와 개수를 지정
  - `1fr`(Fraction):사용 가능한 공간의 비율을 나타내는 유용한 단위
  - 예시:`grid-template-columns: 1fr 1fr 1fr;` (3등분)
  3. **`gap`(또는 `grid-gap`)**
  - 역할:격자 사이의 간격을 설정함
  - 예시:`gap:20px;`(행과 열 간격을 모두 20px로 지정)
  4. **`grid-column`/`grid-row`(아이템 속성)**
  - 역할:개별 아이템이 차지하는 영역의 시작점과 끝점을 지정
  - 예시: `grid-column: 1 / 3;` (1번째 선부터 3번째 선까지 차지)

  ### Flex 와 Grid 차이

|비교항목|Flex(Flexible  Box)|Grid(Grid Layout)|
|------|---------|----------|
|**차원**|**1차원**(가로 또는 세로 중 한방향)|**2차원**(가로와 세로,행과 열 동시 제어)|
|**기준점**|주축과 교차축|행(Row)과 열(Column)|
|**접근 방식**|**콘텐츠 중심:**내용물의 크기에 밪춰 유연하게 배치|**레이아웃 중심:**전체적인 틀을 먼저 잡고 배치|
|**주요 사용처**|네비게이션 바,버튼 그룹|페이지 뼈대,이미지 갤러리,대시보드 위젯|