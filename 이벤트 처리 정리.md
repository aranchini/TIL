# 이벤트 처리
**이벤트란?**
>웹페이지 내에서 발생하는 사용자의 동작(클릭,타이핑,마우스 이동 등)이나 브라우저의 상태 변화를 **이벤트** 라고 함.

**이벤트 처리란?**
>이러한 이벤트라 발생했을 때 그에 맞춰 특정 코드(함수)가 실행되도록 연결하는 과정임

## 이벤트 리스너
- 특정 요소에서 지정한 이벤트가 발생하는지 지켜보다가 사건이 터지면 연결된 함수를 즉시 실행시키는 역할을 함
- 프론트엔드에서는 주로 `addEventListener()`라는 메서드를 사용함
- **기본 문법 구조**
```
대상요소.addEventListener('이벤트종류', 실행할함수, [옵션]);
```
- **대상요소(Target)**:이벤트를 붙일 HTML 요소(버튼,입력창등)
- **이벤트종류(Type)**:어떤 사건을 기다릴것인가(예:`'click'`,`'scoroll'`,`'keyup'`)
- **실행할함수(Handler)**:이벤트가 발생했을 때 수행할 동작



 ### ⚠️ 리스너 사용 시 주의 사항
1. `removeEventListener`를 쓰려면 익명함수 가 **아닌 기명 함수를 사용해야함.**
    >즉 이벤트 리스너를 제거 하려면 반드시 등록할 때 사용했던 함수와 동일한 이름을 가진 함수를 전달해야함.

## 주요 이벤트

- ### 마우스 이벤트(Mouse Events)
    - `click`:요소를 클릭했을 때 사용