# 이벤트 처리
**이벤트란?**
>웹페이지 내에서 발생하는 사용자의 동작(클릭,타이핑,마우스 이동 등)이나 브라우저의 상태 변화를 **이벤트** 라고 함.

**이벤트 처리란?**
>이러한 이벤트라 발생했을 때 그에 맞춰 특정 코드(함수)가 실행되도록 연결하는 과정임

## 이벤트 리스너
- 특정 요소에서 지정한 이벤트가 발생하는지 지켜보다가 사건이 터지면 연결된 함수를 즉시 실행시키는 역할을 함
- 프론트엔드에서는 주로 `addEventListener()`라는 메서드를 사용함
- **기본 문법 구조**
```
대상요소.addEventListener('이벤트종류', 실행할함수, [옵션]);
```
- **대상요소(Target)**:이벤트를 붙일 HTML 요소(버튼,입력창등)
- **이벤트종류(Type)**:어떤 사건을 기다릴것인가(예:`'click'`,`'scroll'`,`'keyup'`)
- **실행할함수(Handler)**:이벤트가 발생했을 때 수행할 동작



 ### ⚠️ 리스너 사용 시 주의 사항
1. `removeEventListener`를 쓰려면 익명함수 가 **아닌 기명 함수를 사용해야함.**
    >즉 이벤트 리스너를 제거 하려면 반드시 등록할 때 사용했던 함수와 동일한 이름을 가진 함수를 전달해야함.

## 주요 이벤트

- ### 마우스 이벤트(Mouse Events)
    사용자가 마우스를 움직이거나 클릭할 때 발생

    - `click`:요소를 클릭했을 때 

    - `dblclick`:더블 클릭했을 때

    - `mouseenter`:마우스 포인터가 요소 위로 올라갔을 때(자식 요소 제외)

    - `mouseleave`:마우스 포인터가 요소 밖으로 나갔을 때

    - `mousemove`:마우스를 움직이는 동안 계속 발생

    - `contextmenu`:마우스 오른쪽 버튼을 눌렀을 때(우클릭 메뉴)
- ### 키보드 이벤트(Keyboard Events)
    사용자가 키보드를 누를 때 발생하며 주로 입력창(`input`,`textarea`)에서 쓰임

    - `keydown`:키를 누르는 순간(모든 키에 반영)

    - `keyup`:누르고 있던 키를 뗄 때

    - `keypress`:키를 누르고 있는 상태 **(** 문자 키 위주 최근엔 `keydown`을 더 권장 **)**
    
- ### 폼 이벤트(Form Events)
    로그인,회원가입 등 데이터를 입력하고 전송 할 때 필수

    - `submit`:폼 제출

    - `reset`:폼 초기화

    - `change`:입력값 변경

    - `input`:입력 발생

    - `focus`:포커스 들어옴

    - `blur`:포커스 빠짐

- ### 창 이벤트(Window Events)
    브라우저 창 자체가 움직이거나 상태가 바뀔 때 사용

    - `load`:리소스 로드 완료

    - `unload`:페이지 종료 등 리소스 언로드

    - `resize`:브라우저 창 크기 변경

    - `scroll`:사용자각 스크롤 할 때

    ### ❗
   **알아두면 좋은것** <br>
   `DOMContentLoaded`
   - **특징:** 이미지나 CSS 파일이 다 다운로드 될 때 까지 기다리지 않고 **HTML 구조만 파악되면 즉시 실행**되기에
    `load`보다 빠름

## 이벤트 객체
### 1. `event`또는`e`
    이벤트가 발생하면 브라우저가 생성해서 함수에 전달해주는 종합 정보 상자 
- **정의:** 발생한 이벤트에 대한 모든정보(클릭 좌표,발생 위치,시간 등)를 담고 있는 객체
- **특징:** 핸들러 함수의 매개변수로 자동 전달됨

### 2. `event.target`
- **정의:** 이벤트가 실제로 발생한 DOM 요소(타깃)를 가리키는 이벤트 객체의 속성
- **활용:** 수많은 버튼 중 어떤 버튼이 눌렸는지 식별할 때 사용

### 3. `e.preventDefault()`
- **정의:**이벤트가 발생했을 때 브라우저가 기본으로 수행하던 동작(예: 링크 이동,폼 전송,새로고침)을 중단 시키는 메서드

- 주로 a태그 클릭 시 페이지 이동을 막거나 form submit 시 새로고침을 막고자 이벤트 핸들러에서 첫 줄에 호출 함