# HTTP 기본 개념

### HTTP / HTTPS
- **HTTP** 란 하이퍼 텍스트 전송 프로토콜로 서버/클라이언트 모델을 따라 데이터를 주고받기 위한 프로토콜이다.
- **HTTPS** 란 HTTP의 확장버전으로 보안이 강화된 버전이다.
### 차이점
|비교항목|HTTP|HTTPS|
|-------|------|------|
|용도|이전 텍스트 기반 웹 사이트|모든 최신 웹사이트|
|보안|추가 보안기능 없음|공개키 암호화에 SSL 인증서 사용|
|주소 시작|http://|https://|
|포트|80|443|

**두개의 가장 큰 차이점은 보안이다.**

### HTTPS가 필요한 이유
- **개인정보 보호**:비밀번호,결제정보 등 민감한 데이터가 암호화되어 노출 위험이 줄어듬
- **데이터 무결성**:전송중 변조(중간자 공격)를 방지해 데이터가 정확히 도착하도록 도움
- **신원증명**:인증서로 서버를 검증해 안전한 사이트로 인식됨

### 요청과 응답 구조
|구분|Request (요청)|Response (응답)|
|-----|-------------|--------|
첫 번째 줄|Start Line (Method + URL + 버전)|Status Line (버전 + 상태코드 + 문구)
헤더 (Headers)|"클라이언트 정보| 목적지 정보 등"|"서버 정보| 데이터 타입(Content-Type) 등"|
바디 (Body)|서버로 보낼 데이터 (POST 등에서 사용)|"클라이언트가 화면에 그릴 데이터 (HTML, JSON 등)"|

### HTTP Method
- `GET`**(조회)**:서버에서 데이터를 가져올때 사용<br>
    - 특징:URL 뒤에 데이터를 붙여서 보냅니다.캐싱(저장)이 가능함
- `POST`**(생성)**:서버에 데이터를 새로 등록할 때  사용합니다.
    - 특징:URL에 노출되지 않게 Request Body에 데이터를 숨겨서 보냅니다.
- `PUT`**(수정/덮어쓰기)**:서버의 기존 데이터를 전체 수정하거나 없으면 새로 생성합니다.
    - 특징: 요청한 데이터로 통째로 갈아 끼웁니다.
- `DELETE`**(삭제)**:서버의 특정 데이터를 삭제할 때 사용합니다.

### 상태 코드

#### 성공

- `200 OK`: 요청이 아주 성공적으로 잘 처리됨.
- `201 Created`: `POST` 요청 등으로 인해 서버에 새로운 리소스가 성공적으로 생성됨.

#### 클라이언트 오류
- `400 Bad Request`: 요청 자체가 잘못됨. (파라미터 오타나 필수 데이터 누락 등)
- `401 Unauthorized`:` 로그인 안 됨. (인증이 필요함)
- `403 Forbidden`: 로그인은 했으나, 이 페이지를 볼 권한이 없음. (예: 일반 회원이 관리자 페이지 접속 시도)
- `404 Not Found`: 요청한 주소(URL)를 찾을 수 없음. (주소창 오타,오래되서 사라짐 등)

#### 서버 오류
- `500 Internal Server Error`: 서버 내부에서 코드 에러나 예외가 발생해 터짐. (개발자가 코드를 고쳐야 하는 상황)